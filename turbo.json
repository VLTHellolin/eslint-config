{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "cache": true
    }
  },
  "globalDependencies": [
    "tsconfig.json",
    "eslint.config.ts",
    "commitlint.config.js",
    "lint-staged.config.js"
  ]
}
